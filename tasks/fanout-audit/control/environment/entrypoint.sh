#!/bin/bash
# Workspace files are generated by the tracker sidecar, not this image.
set -euo pipefail
for _ in $(seq 1 60); do
  if [[ -f /app/src/app.ts ]]; then
    exec "$@"
  fi
  sleep 0.5
done
echo "infra: /app/src/app.ts never appeared" >&2
exit 1
